/*
 * Gradle build script for kotlin command line applications
 *
 * This file was generated by the ReqSmith::forge.
 */

version = "<version>"

plugins {
    application
<plugins>
}

repositories {
    mavenCentral()
}

dependencies {
<dependencies>
}

kotlin {
    sourceSets["main"].apply {
        kotlin.srcDir("build/src/main/kotlin")
        resources.srcDir(listOf("build/src/main/resources"))
    }
}

application {
    applicationName = "<projectName>"
    version = project.version
    mainClass.set("<mainClass>Kt")
}

tasks.withType\<ProcessResources>() {
    exclude("src/main/resources")
    duplicatesStrategy = DuplicatesStrategy.WARN
}
